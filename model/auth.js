import { prisma } from "../prisma/prisma.js";
import { runWithPrisma } from "../prisma/prisma.js";

export class UsuarioAuth {
    // static async getById({ id }) {
    //     return runWithPrisma(async () => {
    //         return prisma.usuario.findUnique({ where: {id: id }})
    //     })
    // };    
}
